spring.application.name=userservice
spring.datasource.url=jdbc:postgresql://postgres:5432/${US_POSTGRES_DB}
spring.datasource.username=${US_POSTGRES_USER}
spring.datasource.password=${US_POSTGRES_PASSWORD}